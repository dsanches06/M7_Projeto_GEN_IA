# Production environment - use a relative API path if backend is served from the same deployment
# If your backend is deployed separately, replace this with its full URL.
VITE_BACKEND_URL=/api

# API Keys are configured in Vercel Environment Variables
# Set these in Vercel Dashboard Project Settings > Environment Variables
# VITE_GROQ_API_KEY=*SET_IN_VERCEL*
